[sections:initcalls]
entries:
    .initcalls

[scheme:initcalls]
entries:
    initcalls -> flash_rodata

[mapping:initcalls]
archive: libmain.a
entries:
    * (initcalls) emit(initcalls) align(8) keep
